---
import type { Lang } from "@/i18n/defaultLangOptions";

type Props = {
  name: string;
  color?: string;
};

const { name, color } = Astro.props;
---

<svg style={color ? `color: #${color}` : undefined}>
  <use href={`/sprite.svg#${name}`}></use>
</svg>
